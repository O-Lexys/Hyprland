
# #######################################################################################
# AUTOGENERATED HYPRLAND CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hyprland.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################


# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor = eDP-1,1920x1080 @144, 0x0, 1, bitdepth, 8, cm, hdredid
#monitor = HDMI-A-1, 1920x1080, 0x-1080, 1, bitdepth, 8, cm, hdredid
#monitor = phone,500x1080, auto-left, 1, bitdepth, 8, cm, hdredid

xwayland {
  force_zero_scaling = true
}



###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = kitty yazi
$menu = krunner 


#################
### AUTOSTART ###
#################
exec-once = qpwgraph
exec-once = noctalia-shell
exec-once = bash -c 'until dbus-send --session --print-reply --dest=org.freedesktop.DBus /org/freedesktop/DBus org.freedesktop.DBus.ListNames 2>/dev/null | grep -q StatusNotifierWatcher; do sleep 0.5; done && carla /home/lioha/rootcustom/carla/noise.carxp'
# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at laun:help |clipboard|

# exec-once = $terminal
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox
#############
### Sound ###
#############


#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/
env = XCURSOR_THEME,Nordzy-cursors
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = QT_QPA_PLATFORMTHEME,qt6ct

###################
### PERMISSIONS ###
###################

# See https://wiki.hyprland.org/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 5
    gaps_out = 10

    border_size = 2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(255,255,255,0.5)
    col.inactive_border = rgba(90,90,90,0.5)
    

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = scrolling
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 10
    rounding_power = 10

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = false
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 1
        passes = 3

        vibrancy = 0.1696
#        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1
    bezier = overshoot, 0.05, 0.9, 0.1, 1.1
    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = global, 1, 10, default
    animation = border, 1, 6, myBezier
    animation = windows, 1, 6, myBezier, popin
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 6, myBezier, slide
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 6, myBezier
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 2.34, overshoot, slide
    animation = workspacesIn, 1, 1.49, overshoot, slide
    animation = workspacesOut, 1, 2.34, overshoot, slide
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us,ua,de
    kb_options = grp:win_space_toggle
    kb_model =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
    force_no_accel=1
    touchpad {
        disable_while_typing = false
        natural_scroll = true
    }
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = 1
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier
$ipc = noctalia-shell ipc call

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Q, exec, $terminal
bind = $mainMod, A, exec, dolphin
bind = $mainMod, C, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, R, exec, $ipc launcher toggle
bind = $mainMod, Escape, exec, $ipc systemMonitor toggle
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, T, exec, $ipc controlCenter toggle
bind = $mainMod, J, togglesplit, # dwindl
#bind = $mainMod, D, exec, zen-browser -new-window https://discord.com/channels/@me

bind = SUPER, space, exec, /home/lioha/rootcustom/scripts/language.sh

bind = $mainMod, L, exec, $ipc lockScreen lock
bind = $mainMod, W, exec, $ipc wallpaper toggle
bind = $mainMod, D, exec, ~/rootcustom/scripts/screenshot_area.sh
#bind = $mainMod, Tab, exec, ~/rootcustom/scripts/sound.sh
#bind = $mainMod, Tab, exec, kitty ollama run llama3
bind = $mainMod, Tab, exec, kitty /home/lioha/rootcustom/server/run.sh
bind = Alt, 2, exec, killall paplay

bind = $mainMod, x, exec, kitty /home/lioha/rootcustom/assistant/assitant.sh
bind = Alt, 4, exec, kitty ~/rootcustom/assistant/assistant.sh


bind = $mainMod, left,  exec, hyprctl dispatch focus l
bind = $mainMod, right, exec, hyprctl dispatch focus r
bind = $mainMod, up,    exec, hyprctl dispatch focus u
bind = $mainMod, down,  exec, hyprctl dispatch focus d

bind = SUPER, B, exec, ~/.local/bin/msi-fan

bind = ALT, 1, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bind = , XF86AudioPlay, exec, /home/lioha/rootcustom/scripts/mut.sh

bind = WIN, F, fullscreen, 0 #fulscreen to entire screen

bind = WIN SHIFT, F, fullscreen, 1 #fulscreen with waybar showing

bind = WIN CTRL, F, fullscreenstate, 0 2 #fullsceeenstate of an app but in any window size

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10
#bind = $mainMod SHIFT, W, exec, /home/lioha/.config/waybar/win.sh
#bind = $mainMod, Tab, overview:toggle

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic2
bind = $mainMod SHIFT, S, movetoworkspace, special:magic2

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous


##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule
# windowrule = float,class:^(kitty)$,title:^(kitty)$

# Ignore maximize requests from apps. You'll probably like this.

# Fix some dragging issues with XWayland
#windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

layerrule {
  name=noctalia-notifications
  blur=true
  blur_popups=true
  ignore_alpha=0.001000
  match:namespace=.*noctalia-notifications.*$
  no_screen_share=true
}



#windowrule = match:class (.*youtube_music.*), pin yes
#windowrule = match:class (.*youtube_music.*), float yes
#windowrule = match:class (.*youtube_music.*), move (monitor_w*1)-360 30
#windowrule = match:class (.*youtube_music.*), size 360 690
#windowrule = match:class (.*youtube_music.*), animation slide right
#windowrule = match:class (.*youtube_music.*), match:float yes, border_color rgba(00000000)
#windowrule = match:class (.*youtube_music.*), match:float yes, border_size 2

#windowrulev2 = float,class:^(.*Spotify.*)$
#windowrulev2 = move 12 44 ,class:^(.*Spotify.*)$
#windowrulev2 = size 1090 1024,class:^(.*Spotify.*)$


#windowrulev2 = opacity 1,class:^(.*LM Studio. *)$
#windowrulev2 = pin,class:^(.*LM Studio.*)$
#windowrulev2 = float,class:^(.*LM Studio.*)$
#windowrulev2 = move 100%-736 44,class:^(.*LM Studio.*)$
#windowrulev2 = size 735 550,class:^(.*LM Studio.*)$


#windowrulev2 = pseudotile, class:^(.*LM Studio.*)$
#windowrulev2 = size 735 550, class:^(.*LM Studio.*)$
#windowrulev2 = move 50 50, class:^(.*LM Studio.*)$


###############
### Plugins ###
###############


plugin {
    hyprwinwrap {
        class = kitty-bg
    }
}

source = /home/lioha/.config/hypr/noctalia/noctalia-colors.conf

